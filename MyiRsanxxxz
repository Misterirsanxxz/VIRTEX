apt update && apt upgrade
$ apt install git
$ apt install curl
$ apt install figlet
$ apt install ruby
$ gem install lolcat
$ pkg install git
$ git clone https://github.com/Mr-xDODOL/VIRTEX
$ cd VIRTEX
$ chmod +x virtex.sh
$ sh virtex.sh
4. Setelah menuliskan perintah
